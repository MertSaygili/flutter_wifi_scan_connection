name: flutter_wifi
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ui
  cupertino_icons: ^1.0.2

  # wifi
  wifi_scan: ^0.4.1
  wifi_iot: ^0.3.19

  # permission handler
  permission_handler: ^11.1.0

  # modules
  # wifi_connector:
  #   path: modules/wifi_connector
  wifi_connector:
    path: modules/wifi_connector


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true